"use client";

import { QuizArena } from "@/components/quiz-arena/QuizArena";

// The Quiz Arena screen: start screen, question flow and results view, all
// rendered by the QuizArena component against the quiz API routes.
export default function QuizPage() {
  return <QuizArena />;
}
