# Firebase Admin service account. This is a server-only secret used to verify
# Firebase ID tokens and assign roles. Never expose it to the Vite frontend.
FIREBASE_SERVICE_ACCOUNT_JSON=

# Required by the existing Realtime Database store when persistence is enabled.
FIREBASE_DATABASE_URL=

# Server-only AI configuration.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
